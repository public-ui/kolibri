## Success-Toast

<kol-button id="showSuccess" _label="Success-Toast-Message anzeigen" _cta="secondary"></kol-button>

<Canvas>
	<div>
		<KolToast _heading="The quick brown fox jumps over the lazy dog." _text="The quick brown fox jumps over the lazy dog." _type="success" />
	</div>
</Canvas>

## Error-Toast

<kol-button id="showError" _label="Error-Toast-Message anzeigen" _cta="secondary"></kol-button>

<Canvas>
	<div>
		<KolToast _heading="The quick brown fox jumps over the lazy dog." _type="error" />
	</div>
</Canvas>

## Info-Toast

<kol-button id="showInfo" _label="Info-Toast-Message anzeigen" _cta="secondary"></kol-button>

<Canvas>
	<div>
		<KolToast _heading="The quick brown fox jumps over the lazy dog." _type="info" />
	</div>
</Canvas>

## Warning-Toast

<kol-button id="showWarning" _label="Warning-Toast-Message anzeigen" _cta="secondary"></kol-button>

<Canvas>
	<div>
		<KolToast _heading="The quick brown fox jumps over the lazy dog." _type="warning" />
	</div>
</Canvas>

<KolToast _heading="Hi, ich bin die Toast-Message in KoliBri! ." _type="success" />

<script>
	() =>
	{setTimeout(() => {
		let buttonSuccess = document.querySelector('#showSuccess');
		let buttonSuccess2 = document.querySelector('#showSuccess2');
		let buttonError = document.querySelector('#showError');
		let buttonInfo = document.querySelector('#showInfo');
		let buttonWarning = document.querySelector('#showWarning');
		buttonSuccess._on = {
			onClick: (event) => {
				document.querySelector('#showToast').innerHTML =
					'<kol-toast-message _heading="Hi, ich bin eine Toast-Message vom Typ Success" _type="success">Und gleich verschwinde ich wieder.</KolToast>';
			},
		};
		buttonSuccess2._on = {
			onClick: (event) => {
				document.querySelector('#showToast').innerHTML =
					'<kol-toast-message _heading="Hi, ich bin eine Toast-Message vom Typ Success" _type="success">Und gleich verschwinde ich wieder.</KolToast>';
			},
		};
		buttonError._on = {
			onClick: (event) => {
				document.querySelector('#showToast').innerHTML =
					'<kol-toast-message _heading="Hi, ich bin eine Toast-Message vom Typ Error" _type="error">Und gleich verschwinde ich wieder.</KolToast>';
			},
		};
		buttonInfo._on = {
			onClick: (event) => {
				document.querySelector('#showToast').innerHTML =
					'<kol-toast-message _heading="Hi, ich bin eine Toast-Message vom Typ Info" _type="info">Und gleich verschwinde ich wieder.</KolToast>';
			},
		};
		buttonWarning._on = {
			onClick: (event) => {
				document.querySelector('#showToast').innerHTML =
					'<kol-toast-message _heading="Hi, ich bin eine Toast-Message vom Typ Warning" _type="warning">Und gleich verschwinde ich wieder.</KolToast>';
			},
		};
	}, 1000)}
</script>
