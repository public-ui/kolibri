import { Meta } from '@storybook/addon-docs';

<Meta title='Changelog' />

<kol-heading _level='{1}'>Changelog</kol-heading>

Auf dieser Seite finden Sie alle Änderungen an KoliBri.

{{#each releases}}
	<kol-heading _level='{2}'>
		{{title}}{{#if tag}} - {{isoDate}}{{/if}}
	</kol-heading>

	{{#if summary}}
		{{summary}}
	{{/if}}

	{{#if merges}}
		<kol-details _summary='Merges' _open>
			<ul>
				{{#each merges}}
					<li>{{#if commit.breaking}}**Breaking change:** {{/if}}{{message}} #{{id}}</li>
				{{/each}}
			</ul>
		</kol-details>
	{{/if}}

	{{#if fixes}}
		<kol-details _summary='Fixes' _open>
			<ul>
				{{#each fixes}}
					<li>{{#if commit.breaking}}**Breaking change:** {{/if}}{{commit.subject}}{{#each fixes}} #{{id}}{{/each}}</li>
				{{/each}}
			</ul>
		</kol-details>
	{{/if}}

	{{#if commits}}
		<kol-details _summary='Commits'>
			<ul>
				{{#each commits}}
					<li>{{#if breaking}}**Breaking change:** {{/if}}{{subject}} #{{shorthash}}</li>
				{{/each}}
			</ul>
		</kol-details>
	{{/if}}
{{/each}}