import './theme-registration';

if (location.pathname.endsWith('toaster.html')) {
	import('./toaster');
}
if (location.pathname.endsWith('reprod-accordion-does-not-toggle-with-open.html')) {
	import('./reprod-accordion-does-not-toggle-with-open');
}
