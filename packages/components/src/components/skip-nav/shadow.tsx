/**
 * @deprecated `kol-skip-nav` was migrated to the skeleton architecture. This file is kept as a
 * re-export shim for any internal imports that still reference `./shadow`; it no longer defines
 * a Stencil component. New code must import `KolSkipNav` from `./component`.
 */
export { KolSkipNav } from './component';
