// import { isObject } from '../validator';

// type HtmlElement = {
// 	dataset: DOMStringMap;
// };
// export const isInstanceOfHtmlElement = (value: unknown) => isObject(value) && isObject((value as HtmlElement).dataset);

// type ShadowRoot = {
// 	mode: 'closed' | 'open';
// };
// export const isInstanceOfShadowRoot = (value: unknown) => isObject(value) && ((value as ShadowRoot).mode === 'closed' || (value as ShadowRoot).mode === 'open');
