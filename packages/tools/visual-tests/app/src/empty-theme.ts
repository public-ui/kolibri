/* Fallback stub for the "@kolibri-vt/theme" alias when no THEME_MODULE is provided (e.g. local
   debugging via `pnpm serve`). During visual testing the alias resolves to the real theme module. */
export {};
