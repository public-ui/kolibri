/* The theme under test is provided at build time through the "@kolibri-vt/theme" Vite alias
   (see vite.config.ts). It resolves to the THEME_MODULE path, so there is no static type for it. */
declare module '@kolibri-vt/theme';
